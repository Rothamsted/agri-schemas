# Rule Name: rdfs:subClassOf+ transitive closure
#
INSERT
{
	GRAPH ${TARGET_GRAPH} {
  	?i a ?targetClass
	}
}
WHERE
{
	{ ?srcClass rdfs:subClassOf ?targetClass }
	FILTER ( ?srcClass != ?targetClass )

	{ ?i a ?srcClass }
}
