INSERT
{
  ?y ?targetProp ?x.

  GRAPH ${TARGET_GRAPH} {
    ?y ?targetProp ?x
  }
}
WHERE
{
  { ?srcProp owl:inverseOf ?targetProp }
  FILTER ( ?srcProp != ?targetProp )
  FILTER ( TRUE || STRSTARTS ( STR(?targetProp), STR (${TARGET_NS}) ) )

  { ?x ?srcProp ?y }
}
