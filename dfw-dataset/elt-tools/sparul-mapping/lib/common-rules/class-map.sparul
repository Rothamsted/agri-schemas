INSERT
{
	GRAPH ${TARGET_GRAPH} {
  	?i a ?targetClass
	}
}
WHERE {
	{ ?srcClass (owl:equivalentClass|rdfs:subClassOf|^owl:equivalentClass|^rdfs:subClassOf)+ ?targetClass }
	FILTER ( STRSTARTS ( STR(?targetClass), STR (${TARGET_NS}) ) )

	{ ?i a ?srcClass }
}
