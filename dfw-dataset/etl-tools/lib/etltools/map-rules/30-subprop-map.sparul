# Rule Name: rdfs:subPropertyOf transitive closure
#
INSERT
{
  GRAPH ${TARGET_GRAPH} {
    ?x ?targetProp ?y
  }
}
WHERE
{
  { ?srcProp rdfs:subPropertyOf ?targetProp }
  FILTER ( ?srcProp != ?targetProp )

  { ?x ?srcProp ?y }
}
